# 乐谱编辑第一版

状态：2026-09-05 实现完成，待用户验收。开发分支：`feat/score-editor`。本次仅本地 commit，push 需用户最终确认。

近期更新：Trainer 的 Practice 回放现与 Editor 使用同一套播放进度、拍位、定位、播放／暂停和 BPM 控件；目标短句连续播放时，谱表显示播放光标。状态栏保持在谱表卡片外。

Trainer 与 Editor 共用最大 1760px 的工作区画布宽度，谱表、控制区、播放器和状态区按同一左右边界排列。

Editor 中白键按默认自然音显示，不额外绘制还原号；黑键显示升号。还原号将随未来的小节内临时记号规则按需绘制。

界面使用标题栏最右侧的 `Trainer / Editor` 切换工作区，标题同步显示 `Piano Trainer / Score Editor`；其左侧固定提供 Theme 与 Settings，两页均可使用。Theme 面板提供 Follow System、颜色与音符着色设置；Settings 仅保留自动保存、Web Sound、Key Labels 与保存／重置设置，练习和五线谱显示则通过 Trainer 的独立控件配置。Trainer 与 Editor 都采用上方控制区、中部谱表和下方状态区的连续卡片布局，Trainer 的 Display Settings、Practice 和 Metronome 位于大谱表正上方。Practice 模式下，谱表下方的“练习回放”可依次播放当前 4 小节目标短句，并支持拖动进度；自由弹奏时该控件禁用。Editor 将名称、文件操作和撤销 / 重做集中在紧凑文档栏，乐谱在主区域内加宽并居中。谱面上方固定显示“输入 / 当前”两行，两行共用音高、时值、音符操作和休止符操作四列，输入时值和当前时值均通过左右箭头在五档时值间步进。乐谱按拍格分配横向空间，长时值占用更多宽度，短时值维持紧凑且可读的最小间距。编辑光标与播放光标独立：编辑位置仅在越出可视区域中央 60% 的安全区后平滑跟随；播放期间使用同一安全区，并按播放帧连续横向跟随，不叠加平滑滚动动画。删除当前事件后自动选中其后的事件，以便在原时间位置连续编辑。乐谱纵向边界会完整容纳 C8 的上行符干与 A0，并保留紧凑的时值标签区。页面主滚动条在视觉上隐藏，但鼠标、触控板、触摸与键盘滚动保持可用；打开设置浮层不会改变页面宽度。

## 使用流程

1. 打开「乐谱编辑」选项卡。底部钢琴、电脑键盘、USB MIDI、Bluetooth MIDI 继续共用。
2. 自由试音：单音可点击琴键；和弦使用电脑键盘或 MIDI 同时按键。按下新音时暂存当时同时按住的音高，松键后保留最近试音。完全松开再试弹可开始新的一组。最近试音会以强调色音符显示在当前插入锚点，并随待写入时值变化；预览在自己的虚拟时值格内居中，但不参与排版或推移已有内容，点击「写入音符」后才进入乐谱数据。
3. 在“插入”行使用输入时值两侧的箭头选择时值，点击「写入音符」或「写入休止符」。时值使用 `1/8 音符 | 1/2 拍` 的分数格式，前者是记谱时值，后者是 4/4 下的实际拍数。未选中已有音符时在末尾追加，选中时插在该项之前。输入时值只控制预览和新事件。
   “输入时值 / 当前时值”及最长的分数文案始终单行显示；窄窗口通过编辑栏横向滚动保留完整文本，不会将中文标签逐字换行。
   已有事件被选中时以着色音符和谱面上方短线表示；待写入内容以高亮音符和插入竖线表示，两种状态可以同时存在但含义不同。
4. 点击谱上的音符后，“当前”行显示原音高和当前时值。先在底部琴键试出新音，再点击「替换音符」修改音高；替换音高不会改变当前时值。也可替换为休止符、删除或回到末尾。不再通过下拉框逐个调整音高。
   右侧辅助区以固定 2×2 网格提供“回到开头 / 回到末尾 / 删除全部 / 删除当前”，不增加两行编辑栏高度。删除全部第一次点击变为红色“确认删除”，再次点击才执行；点击任何其他界面操作会取消确认状态。所有单元格保留固定位置；窗口较窄时编辑栏横向滚动，不换行增加高度。
   输入音文字、待写入谱面音符与插入标记使用主题强调色；当前音文字、谱面选中音符与选择短线使用主题蓝色。其余已写入音符保持普通谱面颜色。
5. 乐谱下方提供播放进度、拍位、定位准星、单个播放 / 暂停按钮和 BPM 控件。拖动进度条不改变编辑选择；播放按钮旁的准星把播放指针放在当前事件开头，没有选择事件时禁用。速度支持 30–240 BPM，可通过两侧按钮每次调整 10，也可直接输入后按 Enter 或移开焦点生效。修改乐谱或速度会暂停播放。
6. 点击「保存」保存到当前浏览器，下次刷新自动恢复。需要多份乐谱或备份时使用「导出」，通过「打开文件」重新打开 `.piano.json` 文件。

保存按钮在内容与最后一次本地保存不同时使用主题高亮色，保存成功后恢复普通样式；按钮文字和宽度保持不变。离开网页时会提示未保存内容。新建 / 打开文件会替换工作区内容，但可以撤销；更换文件后需再次保存，才会替换本地草稿。
撤销和重做最多保留最近 50 次编辑，刷新后不保留。乐谱保存独立于 Application Settings 的 Auto Save 设置。

按住实奏琴键时，先松键再切换选项卡。切换到 Trainer 或网页进入后台时回放暂停；重新进入编辑区不会自动播放。
长乐谱横向滚动，小窗口可上下滚动工作区，底部键盘保持固定。

## 当前支持范围

- 固定 4/4、一个连续声部，四分音符为 1 拍。
- 全音符、二分、四分、八分、十六分五种时值。
- 单音、同时起止的和弦与休止符；和弦不是仅限大 / 小三和弦。
- A0–C8（MIDI 21–108），每个事件最多 12 个音，单谱最多 2000 个事件，导入文件最多 2 MB。
- 双谱表按音域自动分配；一个和弦可跨高低谱表，但仍是同一声部、同一时值。
- Editor 与 Trainer 使用相同的大谱表垂直几何关系，中央 C 位于高低音谱内侧线正中。
- 跨小节的长音拆分显示并画延音线，回放保持原有完整时值。
- 升号拼写与显式还原号；未实现降号、调号或等音拼写选择。
- 浏览器原有 Web Audio 合成音色及 Web Sound 开关。

## 数据格式

```json
{
  "version": 1,
  "title": "旋律草稿",
  "tempo": 90,
  "timeSignature": [4, 4],
  "events": [
    { "id": "note-1", "startBeat": 0, "duration": 1, "pitches": [60] },
    { "id": "chord-1", "startBeat": 1, "duration": 2, "pitches": [60, 64, 67] },
    { "id": "rest-1", "startBeat": 3, "duration": 1, "pitches": [] }
  ]
}
```

`startBeat` 从 0 开始；界面选择信息显示从 1 开始的拍位，播放进度表示从 0 起的已播放拍数。
`pitches` 保存 MIDI 音高；空数组表示休止符。数据不包含谱面坐标、琴键状态、设备连接或 Trainer 目标。

当前事件必须从 0 起连续排列，ID 唯一，和弦内音高不重复。增删时值会重新计算后续拍位，但不改变既有事件 ID。
导入会检查版本、拍号、速度、时值、连续性、ID、音域和数据大小，并只保留规范字段；失败时不替换当前乐谱。
本地存储键为 `piano-trainer.score.v1`，存储异常会显示提示而不自动覆盖原内容。

未来简谱可从音高与调性派生，吉他六线谱还需弦位 / 品位与可演奏性信息。扩展声部、调性和节拍时应增加数据版本并迁移 v1，而不是把 SVG 作为原始乐谱。

## 代码与边界

| 文件 | 职责 |
| --- | --- |
| `src/score/scoreModel.ts` | 文档类型、编辑操作、导入校验和音乐时间 |
| `src/score/notation.ts` | 跨小节显示片段，与实际回放事件分离 |
| `src/score/ScoreStaff.tsx` | 可选择的基础 SVG 双谱表及插入位置试音预览 |
| `src/data/staffGeometry.ts` | Trainer / Editor 共用的大谱表纵向几何 |
| `src/score/scoreTransport.ts` | 单调时钟、进度、暂停、定位和回放声音释放 |
| `src/score/ScoreEditor.tsx` | 编辑操作、历史、本地存储和文件导入导出 |
| `src/score/score.css` | 编辑区样式，使用现有主题变量 |
| `src/App.tsx` | 选项卡、实奏路由和共享键盘集成 |

回放通过 Input Layer 的 `playback` 来源进入 App，以 `score:` 前缀区分音频实例；因此同音试弹和回放不会互相释放。
回放仅更新回放高亮，不产生 Trainer 事件、不更改最近试音。连续相同音符以事件 ID 区分，按时间边界重新发音。

Trainer 的 PracticeController、Target Lifecycle、生成器、GrandStaff、MIDI / BLE Parser 均保持原实现。
共享输入适配只新增表单输入 / 快捷键 / 重复键防误触，音频只新增同名声音重复启动保护。

## 验证

使用 Node.js 24：

```sh
npm test
npm run build
npm run lint
```

12 项自动化测试覆盖：

- 插入、时值修改、删除的连续拍位和 ID 保留。
- 单音、和弦、休止符文件往返及非法数据拒绝。
- 跨小节显示时值守恒与延音。
- 连续同音重奏、休止符静音、播放中定位、暂停后定位与恢复。
- 停止、结束后重播、空谱、编辑中止播放及定时器延迟处理。
- 表单 / 快捷键防误触、按键释放、重复音频保护和回放声音隔离。

浏览器验收覆盖单音与电脑键盘和弦试音 / 写入、音高和时值修改、本地保存后刷新恢复、JSON 文件导入、极端音域显示、播放暂停与定位、选项卡切换。USB / Bluetooth MIDI 硬件未在本次重新连接验证；已有设备适配代码未变。

## Technical Debt 与后续方向

- 基础 SVG 排版尚未达到印刷级；密集和弦避让、连梁、自动分行和长谱渲染优化仍需完善。
- 回放通过前台定时器驱动，单调时钟避免累计漂移，但高负载可能导致抖动或跳过过短事件；后续考虑音频时钟提前调度。
- 本地仅保留一份草稿，多文档暂由文件导入导出管理；未加入云端同步、自动保存或谱库。
- 原曲音频导入 / 降速 / 区间循环、附点、三连音、多声部、调号、简谱和吉他谱转换尚未实现。
- MusicXML、MIDI 文件导入导出和 PDF 排版不属于本版。
- 既有多实奏输入源同时按住同音的 ownership / reference counting 问题仍保留；本次只隔离回放与实奏，未重构整个输入层。
